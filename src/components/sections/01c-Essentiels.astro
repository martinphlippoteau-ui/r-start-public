---
/**
 * Section 1c, bandeau « Points essentiels à connaître » (id « points-essentiels »), sur l'accueil
 * entre « Qui a envie de payer avant de gagner ? » (01b, paper) et « R Start en un clin d'œil »
 * (02, paper). Ajouté le 22/09/2026 (demande de Martin) : la mention de CORUM L'Épargne, dans le
 * bandeau commun (ui/EssentialsBand.astro). Rien d'autre.
 * Contenu : src/content/fr/essentials.ts.
 */
import EssentialsBand from '@/components/ui/EssentialsBand.astro';
import { sections } from '@/config/sections';
import { essentials } from '@/content/fr/essentials';
---

<EssentialsBand id={sections.essentials.id} title={essentials.title}>
  <p>{essentials.text}</p>
</EssentialsBand>
