---
/**
 * Page /outils (12/09/2026) : la porte d'entrée des quatre outils chiffrés du site.
 * SiteNav → main#contenu : PageHero (fil d'Ariane, H1, ligne risques visible sans défiler) → cadre qui
 * pose la règle commune aux quatre outils → quatre cartes, une par outil → notes → Footer.
 *
 * Chaque outil vit sur SA page, sous /outil : la page d'accueil ne fait que poser la question à laquelle
 * il répond. Voir src/content/fr/toolsPage.ts pour le détail et pour les deux idées écartées.
 *
 * Règle de la page : aucun outil ne projette de performance. Ils appliquent les taux du contrat aux
 * montants saisis par le visiteur, et le cadre le dit avant le premier calcul.
 */
import Base from '@/layouts/Base.astro';
import SiteNav from '@/components/SiteNav.astro';
import Footer from '@/components/Footer.astro';
import LegalNotes from '@/components/LegalNotes.astro';
import ToolsGate from '@/components/pages/outils/ToolsGate.astro';
import Breadcrumb from '@/components/ui/Breadcrumb.astro';
import Container from '@/components/ui/Container.astro';
import PageHero from '@/components/ui/PageHero.astro';
import Section from '@/components/ui/Section.astro';
import ToolCard from '@/components/pages/outils/ToolCard.astro';
import { pages } from '@/config/pages';
import { notes, toolCards, toolsPage } from '@/content/fr/toolsPage';
import { breadcrumbJsonLd, organizationJsonLd, webPageJsonLd } from '@/lib/seo';

const page = pages.tools;
const { seo, hero, index } = toolsPage;

const jsonLd = [
  organizationJsonLd(),
  webPageJsonLd({ path: page.path, title: seo.title, description: seo.description }),
  breadcrumbJsonLd('tools'),
];
---

<Base title={seo.title} description={seo.description} path={page.path} jsonLd={jsonLd}>
  <SiteNav />
  <main id="contenu">
    {
      /* En-tête plus court encore que celui des autres sous-pages (13/09/2026, demande de l'équipe) :
        cette page n'est qu'un sommaire, les quatre cartes doivent être sous les yeux tout de suite. */
    }
    <PageHero eyebrow={hero.eyebrow} title={hero.title} riskLine={hero.riskLine} compact>
      <Breadcrumb slot="breadcrumb" key="tools" tone="dark" />
    </PageHero>

    <Section id="liste" tone="white" labelledBy="liste-title" class="pt-10 sm:pt-12 lg:pt-14">
      <Container size="wide">
        {
          /* Titre et chapeau MASQUÉS VISUELLEMENT (12/09/2026, demande de l'équipe) : les quatre cartes
            se présentent seules, chacune portant déjà sa question. Le titre reste dans le code parce que
            la section a besoin d'un nom : sans lui elle n'est plus annonçable au lecteur d'écran, et
            `labelledBy` pointerait dans le vide. */
        }
        <h2 id="liste-title" class="visually-hidden">{index.title}</h2>

        <ul class="grid gap-6 sm:grid-cols-2 lg:gap-8" role="list">
          {
            toolCards.map((outil) => (
              <li class="h-full">
                <ToolCard
                  href={pages[outil.pageKey].path}
                  eyebrow={outil.card.eyebrow}
                  title={pages[outil.pageKey].label}
                  question={outil.card.question}
                  teaser={outil.card.teaser}
                  cta={index.cardCta}
                />
              </li>
            ))
          }
        </ul>
      </Container>
    </Section>

    <LegalNotes notes={notes} />
  </main>
  <Footer />

  {
    /* Fenêtre d'acceptation : rendue APRÈS le pied de page, en dernier enfant du document. Une modale
      rend le reste inerte, elle n'a donc aucune raison de vivre dans le contenu, et hors de `main` elle
      ne s'intercale pas dans la lecture linéaire de la page une fois acceptée. */
  }
  <ToolsGate />
</Base>
